name: Crawl Job Boards

on:
  schedule:
      - cron: '0 23 * * 1-5'  # Every weekday (Monday-Friday) at 23:00 UTC
  workflow_dispatch:       # Allow manual trigger

permissions:
  contents: write

jobs:
  crawl:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run crawler
        run: python crawl.py

      - name: Generate new jobs manifest
        if: always()
        run: python scripts/generate_new_jobs.py

      - name: Commit changes
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ docs/new_jobs.json docs/index.html
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🦀 crawl: $(date -u +%Y-%m-%d)"
            git push
          fi